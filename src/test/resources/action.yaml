name: 'Start a test_run on desktop'
description: 'Start a testrun on desktop'
inputs:
  project_arn:
    description: 'x'
    required: true
  aws_access_key:
    description: 'x'
    required: true
  aws_secret_access_key:
    description: 'x'
    required: true

runs:
  using: composite

  steps:
    - shell: cmd
      run : 'cd '
    - shell: cmd
      run: 'mvn test'

